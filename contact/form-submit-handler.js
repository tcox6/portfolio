const form = document.getElementById("contactForm");

form.addEventListener('submit', function(e) {
    e.preventDefault();
    // TODO: call ASWS lambda function
    // will need to setup custom domain name for email
    // emails will be sent using AWS SES
    // AWS lambda function is already created - just need to modify some details
});